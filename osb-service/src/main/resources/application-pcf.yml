---
spring:
  rabbitmq:
    host: "${RABBITMQ_HOST}"
  data:
    mongodb:
      host: "${MONGODB_HOST}"
existing:
  endpoint:
    port: 9042
    pcfHosts: "${EXISTING_HOSTS}"
    name: cassandra
#haproxy:
#  uri: "https://${SERVICE_KEY_MANAGER}/agents/${HAPROXY_AGENT_ID}/schemas?type=listen"
